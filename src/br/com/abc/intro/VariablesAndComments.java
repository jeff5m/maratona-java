package br.com.abc.intro;

public class VariablesAndComments {
    // Single line coon number types, the result of an operation between same types will be a number of a same typemment

    /*
        Block com ment
      */

    /***
     * JavaDoc comments, used to generate documentation of classes, methods and attributes. Ex:
     * @param args - entry parameter of main method.
     */
    public static void main(String[] args) {
    /*
        Java primitive types:
          on number types, the result of an operation between same types will be a number of a same type
            1. int -
            2. double -
            3. float -
            4. boolean -
            5. char - 2 bytes. Accept a character, an integer numbers >= 2 bytes end hexadecimal code (char a ='\u0041')
            6. byte -
            7. short -
            8. long -
     */
    }
}
